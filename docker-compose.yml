version: '3.8'

services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: smartcity-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: smartcity
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: smartcity-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment to run TCP server in Docker
  # tcp-server:
  #   build:
  #     context: .
  #     dockerfile: apps/tcp-server/Dockerfile
  #   container_name: smartcity-tcp-server
  #   restart: unless-stopped
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - REDIS_HOST=redis
  #     - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/smartcity
  #   depends_on:
  #     - postgres
  #     - redis

volumes:
  postgres_data:
  redis_data:
